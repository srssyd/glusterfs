before_script:
- ./scripts/install-deps.sh

stages:
- configure
- build
- deploy

configure_job:
    stage: configure
    script:
    - ./autogen.sh
    - ./configure --enable-firewalld

build_job:
    stage: build
    script:
    - make -j

