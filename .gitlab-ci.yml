stages:
- install-dep
- build
- deploy

variables:
    INSTALL_DEP_SCRIPT: "echo 'hpcgrid3102' | su root -c ./srcipts/install-deps.sh"

install-dep-00:
    stage: install-dep
    tags:
      - tstore00
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-01:
    stage: install-dep
    tags:
      - tstore01
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-02:
    stage: install-dep
    tags:
      - tstore02
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-03:
    stage: install-dep
    tags:
      - tstore03
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-04:
    stage: install-dep
    tags:
      - tstore04
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-05:
    stage: install-dep
    tags:
      - tstore05
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-06:
    stage: install-dep
    tags:
      - tstore06
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-07:
    stage: install-dep
    tags:
      - tstore07
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-08:
    stage: install-dep
    tags:
      - tstore08
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-09:
    stage: install-dep
    tags:
      - tstore09
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-10:
    stage: install-dep
    tags:
      - tstore10
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-11:
    stage: install-dep
    tags:
      - tstore11
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-12:
    stage: install-dep
    tags:
      - tstore12
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-13:
    stage: install-dep
    tags:
      - tstore13
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-14:
    stage: install-dep
    tags:
      - tstore14
    script:
    - $INSTALL_DEP_SCRIPT 

install-dep-15:
    stage: install-dep
    tags:
      - tstore15
    script:
    - $INSTALL_DEP_SCRIPT 

build-job:
    stage: build
    script:
    - ./autogen.sh
    - ./configure --enable-firewalld
    - make -j
